fix: remove bun.lock from .dockerignore (broke Docker COPY)

The previous fix changed bun.lockb -> bun.lock in .dockerignore,
which excluded the actual lockfile from the build context. Docker
COPY needs the lockfile for bun install --frozen-lockfile to work.

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>
